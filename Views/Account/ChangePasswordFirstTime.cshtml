@model G_StockVente.Controllers.ChangePasswordFirstTimeViewModel
@{
    ViewData["Title"] = "Changer votre mot de passe";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="d-flex align-items-center justify-content-center" style="min-height: calc(100vh - 160px);">
    <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card" style="border: 1px solid var(--color-border); box-shadow: var(--shadow-lg);">
            <div class="card-body" style="padding: var(--space-8);">
                <!-- Header -->
                <div class="text-center mb-6">
                    <div style="width: 64px; height: 64px; margin: 0 auto var(--space-4); border-radius: var(--radius-xl); background: var(--color-warning-light); display: flex; align-items: center; justify-content: center; font-size: 32px;">
                        🔒
                    </div>
                    <h1 style="font-size: 24px; font-weight: 800; margin: 0 0 var(--space-2);">Sécurisation du compte</h1>
                    <p class="text-muted" style="font-size: 14px; margin: 0;">Première connexion détectée</p>
                </div>

                <div class="alert alert-warning d-flex align-items-start gap-3 mb-5">
                    <div class="alert-icon">⚠️</div>
                    <div>
                        Pour des raisons de sécurité, vous devez définir un nouveau mot de passe avant de continuer.
                    </div>
                </div>

                <form asp-action="ChangePasswordFirstTime" method="post" novalidate id="changePasswordForm">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4"></div>

                    <div class="mb-4">
                        <label asp-for="CurrentPassword" class="form-label" style="font-weight: 600; margin-bottom: var(--space-2);">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: -2px; margin-right: 4px;"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect><path d="M7 11V7a5 5 0 0 1 10 0v4"></path></svg>
                            Mot de passe actuel <span class="text-danger">*</span>
                        </label>
                        <input asp-for="CurrentPassword" 
                               class="form-control" 
                               style="height: 42px; padding: var(--space-3) var(--space-4); font-size: 14px;"
                               placeholder="••••••••" 
                               autofocus 
                               autocomplete="current-password"
                               required />
                        <span asp-validation-for="CurrentPassword" class="form-error"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="NewPassword" class="form-label" style="font-weight: 600; margin-bottom: var(--space-2);">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: -2px; margin-right: 4px;"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect><path d="M7 11V7a5 5 0 0 1 10 0v4"></path></svg>
                            Nouveau mot de passe <span class="text-danger">*</span>
                        </label>
                        <input asp-for="NewPassword" 
                               class="form-control" 
                               style="height: 42px; padding: var(--space-3) var(--space-4); font-size: 14px;"
                               placeholder="••••••••" 
                               autocomplete="new-password"
                               required />
                        <span asp-validation-for="NewPassword" class="form-error"></span>
                        <small class="text-muted" style="font-size: 13px; margin-top: var(--space-1); display: block;">
                            Minimum 6 caractères, avec majuscules, minuscules et chiffres.
                        </small>
                    </div>

                    <div class="mb-5">
                        <label asp-for="ConfirmPassword" class="form-label" style="font-weight: 600; margin-bottom: var(--space-2);">
                            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: -2px; margin-right: 4px;"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg>
                            Confirmer le mot de passe <span class="text-danger">*</span>
                        </label>
                        <input asp-for="ConfirmPassword" 
                               class="form-control" 
                               style="height: 42px; padding: var(--space-3) var(--space-4); font-size: 14px;"
                               placeholder="••••••••" 
                               autocomplete="new-password"
                               required />
                        <span asp-validation-for="ConfirmPassword" class="form-error"></span>
                    </div>

                    <button type="submit" class="btn btn-primary btn-lg w-100" id="submitButton" style="height: 48px; font-weight: 600;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: var(--space-2);"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path></svg>
                        Sécuriser mon compte
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        document.getElementById('changePasswordForm')?.addEventListener('submit', function(e) {
            const button = document.getElementById('submitButton');
            if (button && !button.disabled) {
                LoadingState.show(button);
            }
        });
    </script>
}

